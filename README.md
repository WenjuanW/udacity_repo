# udacity_repo
This is a repository to practice git version control for Udacity MLOps course.

### list of git command

* git add
* git commit -m
* git push
* git pull
* git checkout -b
* git checkout -d

